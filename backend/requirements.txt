# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Supabase (compatible with Pydantic 2.x)
supabase>=2.3.0

# Environment variables
python-dotenv==1.0.0

# HTTP requests
httpx==0.24.1
requests==2.31.0
certifi==2024.2.2
urllib3<2

# AI/ML
groq==0.4.0
# Note: torch CPU-only version installed in Dockerfile to reduce image size
sentence-transformers==2.7.0  # Free embeddings (all-MiniLM-L6-v2)
pgvector==0.2.4  # PostgreSQL vector extension

# Data validation
pydantic==2.9.2
pydantic-settings==2.6.1

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Utilities
python-dateutil==2.8.2

# Caching
cachetools==5.3.2  # Free, lightweight in-memory cache with TTL support
